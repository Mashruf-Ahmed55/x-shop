# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @x-shop/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t x-shop-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system x-shop-auth-service && \
          adduser --system -G x-shop-auth-service x-shop-auth-service

COPY apps/auth-service/dist x-shop-auth-service/
COPY apps/auth-service/package.json x-shop-auth-service/
RUN chown -R x-shop-auth-service:x-shop-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix x-shop-auth-service --omit=dev -f install

CMD [ "node", "x-shop-auth-service" ]
